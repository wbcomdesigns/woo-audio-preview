<?xml version="1.0"?>
<ruleset name="Audio Preview for WooCommerce">
	<description>
		The standard CI enforces. This file exists so `composer lint` and the pre-commit hook run
		the same check the pipeline does - without it, a bare phpcs fell back to PEAR and reported
		hundreds of failures that CI does not care about, which trains everyone to ignore the tool.
	</description>

	<file>.</file>

	<exclude-pattern>/vendor/*</exclude-pattern>
	<exclude-pattern>/node_modules/*</exclude-pattern>
	<exclude-pattern>/build/*</exclude-pattern>
	<exclude-pattern>*.min.js</exclude-pattern>
	<exclude-pattern>*.min.css</exclude-pattern>
	<exclude-pattern>/assets/vendor/*</exclude-pattern>

	<arg name="extensions" value="php"/>
	<arg value="ps"/>

	<rule ref="WordPress-Extra"/>
</ruleset>
